const IdList = ({ documentId }) => {
  console.log(documentId);
  return (
    <>
      {/* {
      documentId.map((id, index) => (
        <button key={index}>{id}</button>;
      )
    )
      } */}
    </>
  );
};

export default IdList;
